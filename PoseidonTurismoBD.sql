create database PoseidonTurismoBD;
use PoseidonTurismoBD;

Create table PoseidonTurismo (id int primary key not null auto_increment,
clientes varchar(20) not null, navios varchar(20) not null, rotas varchar(20), datas varchar (10),pacotes varchar(20));
